import type { VehicleRecord } from '@/types/vehicle'

export type VehicleSummary = VehicleRecord

export type VehicleActionItem = {
  id: string
  label: string
  description: string
}

export type { VehicleTypeOption } from '@/types/vehicle'
